echo "server { listen 80; server_name localhost; }" > nginx.conf
git add nginx.conf
git commit -m "Add basic nginx config"
git push -u origin feature/add-nginx
server { listen 80; server_name localhost; }
